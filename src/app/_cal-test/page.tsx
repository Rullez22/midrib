import { DatepickerDemos } from "../ds/datepicker-demos";

export default function CalTest() {
  return (
    <div style={{ padding: 40, background: "#fff", minHeight: "100vh" }}>
      <DatepickerDemos />
    </div>
  );
}
