import { PoolConnectScreen } from "./_components/pool-connect-screen";

/**
 * Подключение адресов к пул-счету — экран-развилка по «Перевод» в маркетинговом
 * (пул-)счёте. Источник: Figma node 2649:360298.
 */
export default function PoolConnectPage() {
  return <PoolConnectScreen />;
}
