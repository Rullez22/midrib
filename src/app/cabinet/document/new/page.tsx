import { AddDocumentScreen } from "../_components/add-document-screen";

/**
 * Экран «Добавление нового документа» (Figma 6419:313771). Открывается из
 * тулбара таба «Документооборот» (кнопка «Добавить документ»).
 */
export default function AddDocumentPage() {
  return <AddDocumentScreen />;
}
