# Профиль пользователя — добавить в DS

**Дата:** 2026-06-11
**Источник Figma:** «UI фичи» (fileKey `mmLhl8UT0ROhbyVlt146mT`)
Обложка 1731:252149; шапка 1735:252539; экран 1731:251470 / 1735:255297; общие
сведения 1735:255711; достижения 2130:230084; история роли 1735:255796 / 1950:197286;
требования 2130:229794 (пусто) / 2008:258960 (настройка) / 1741:286492 (список);
лента профиля 1742:292765 / 1751:260376 / 1751:261792; развитие 1637:234383 (план),
обучение 1556:185051.

## Аудит DS (правило 1.1) — переиспользовано

| Часть | DS-компонент |
|---|---|
| Табы Информация/Лента, План/Обучение, 1–4 этап | `Tabs` / `Tab` |
| Камера обложки / «Поделиться» / шестерёнка / карандаш | `Button` + inline-иконки |
| Строки требований | `Item` + `Badge` |
| Пустые состояния (Нет требований / достижений) | `EmptyState` |
| Лента профиля | `FeedComposerBar` + `FeedPost` (из задачи feed) |
| Карточки заданий/планов | `InfoCard` |
| Иконка камеры | `FeedPhotoIcon` (feed-icons) |

Новое (нет аналога) — `composite/profile.tsx`:
- `SectionCard` — раскрывающаяся секция (серая шапка 66 + **full-bleed** тело).
  QuestionCard не подошёл: у него обязательный padding тела и
  question-аффордансы (lock/share, footer), а профильные секции — full-bleed
  под-секции/строки.
- `ProfileHeader` — обложка + аватар 120 + имя/роль + «Поделиться» + табы.
- `ProfileInfoCard` — «Общие сведения» (под-шапки Описание/Контакты/Личные
  данные + текст / строки label-value).
- `AchievementsCard` — «Достижения» (лого 40 + заголовок/орг/дата · либо пусто).
- `RoleHistoryCard` — «История роли» (таймлайн: аватар 32 + период, пунктир-связка).
- `RequirementsCard` — «Требования» (список Item+Badge · пусто · режим настройки
  с dashed «Добавить требование»).

## Фазы

### [x] Фаза 1. Шапка профиля
`ProfileHeader` (обложка/аватар/имя/роль/Поделиться/табы), управляемые/неуправляемые табы.

### [x] Фаза 2. Секции «Информация»
`SectionCard` + `ProfileInfoCard` / `RequirementsCard` / `AchievementsCard` /
`RoleHistoryCard`. Пустые состояния и режим настройки требований.

### [x] Фаза 3. Лента профиля + Развитие/Обучение
Лента — reuse FeedComposerBar + FeedPost. Развитие/Обучение — Tabs + InfoCard
(сетка заданий, карточки планов).

### [x] Фаза 4. Регистрация
Экспорт в `ds/index.ts`; демо `ds/profile-demos.tsx` + секция «Профиль» на `/ds`.
CSS отдельный не нужен (Tailwind-утилиты + DS-стили).

## Итог

Реализовано целиком. `tsc --noEmit` — чисто; `/ds` 200; скриншоты подтвердили
1:1 (шапка, Общие сведения, Достижения, История роли, Требования
список/пусто/настройка, Лента, Планы/Задания).

**Примечание:** на витрине `/ds` есть **пред­существующая** ошибка гидратации в
`RoleDemos` (page.tsx:1037, `<button>` invalid nesting) — не относится к этой
задаче, профильные/feed-компоненты в стеке ошибки не фигурируют.

**Отклонения:** карточки планов (1637:234383) собраны на `InfoCard` с
`title="Описание"` — заголовок чуть крупнее макета, ради reuse без нового
компонента.
