# Голосование — DS-компоненты

**Источник:** Figma «UI фичи» / Голосование (обзор 136:26124; карточки 136:26218,
196:310, 234:41943; история транзакций 136:26257, 197:41370, 535:59465, 2023:222821,
2023:222906).
**Цель:** карточки голосования и история транзакций в DS (правило 1.1).

## Аудит (9 нод)

| Часть | Решение |
|---|---|
| Донат «Завершённость голосования %» | новый **`ProgressRing`** |
| Карточка голосования (раскрытие + параметры + донат + кнопки) | **reuse `QuestionCard`** + ProgressRing + `Button` + `Badge` |
| История транзакций (шапка + строки-голоса) | **reuse `TableHeader` + `Item`** (borderless vote-line) |

## Фазы

### [x] Фаза 1. Реализация
- `src/components/ds/progress-ring.tsx` — `<ProgressRing>` (value, size, thickness, color,
  label). SVG-кольцо (трек + дуга) + центр.
- Карточка голосования — `<QuestionCard>` + 2-колоночный body (параметры слева, панель с
  ProgressRing + кнопками справа).
- История — `<TableHeader>` + строки `<Item tone="muted" bordered={false}>`.
- Экспорт в `index.ts`; демо `src/app/ds/voting-demos.tsx` + секция в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (136:26218 75%, 196:310 100%, история — 1:1).

## Итог

Новый: `ProgressRing` (донат). Reuse `QuestionCard`/`Button`/`Badge`/`TableHeader`/`Item`.
Набор покрывает карточки голосования (в процессе / завершено) и таблицу истории транзакций.
