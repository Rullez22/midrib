# Select part (выбор верификации / подключения) — DS

**Источник:** Figma «UI фичи» (966:90156 промо, 2233:231059 обзор, 2233:231062 Международная,
2233:231211 Локальная, 2233:231188 опция).
**Цель:** карточки выбора типа верификации/подключения в DS (правило 1.1).

## Аудит (5 нод)

| Часть | Решение |
|---|---|
| Вариант выбора (заголовок + описание + цветная «Выбрать») | новый **`SelectOption`** (orange/green/blue/red) |
| Карточка категории (Международная/Локальная + 2 опции) | композиция: бордерная карточка + `Text` + 2× `SelectOption` |
| Промо-карточка (иконка + заголовок + кнопка) | композиция: `Text` + `Button` |

## Фазы

### [x] Фаза 1. Реализация
- `src/components/ds/composite/select-option.tsx` — `<SelectOption>` (title, description,
  color, buttonLabel, onSelect).
- Экспорт в `index.ts`; демо `src/app/ds/select-part-demos.tsx` + секция в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (2233:231188/231062/231211 — 1:1).

## Итог

Новый: `SelectOption`. Карточки категорий и промо — композиция из `Text`/`Button`.
