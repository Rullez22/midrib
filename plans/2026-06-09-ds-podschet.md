# Создание подсчёта — DS (только композиция)

**Источник:** Figma «UI фичи» / создание подсчёта (1506:179108 форма, 1507:184133 просмотр).
**Цель:** создание/просмотр подсчёта (под-счёта) в DS (правило 1.1).

## Аудит (2 ноды) — 100% reuse, новых компонентов НЕТ

| Часть | Решение |
|---|---|
| Форма (название, тип, ОКВЭД, назначение, источник) | `Input` · `Radio` · select(`Input`) · `Textarea` |
| Распределение (Целевой счёт + токены, %) | `IncrimentField` |
| Правая панель (процент + чекбоксы) | `ProgressRing` + `Checkbox` |
| Просмотр (label/value) | `MemberCard` + `Badge` |

## Фазы

### [x] Фаза 1. Композиция
- `src/app/ds/podschet-demos.tsx` — форма + просмотр из готовых DS-компонентов.
- Секция «Создание подсчёта» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (1506:179108 — 1:1).

## Итог

Новых DS-компонентов не вводилось — фича собрана из существующих
(`Input`/`Radio`/`Textarea`/`IncrimentField`/`ProgressRing`/`Checkbox`/`MemberCard`/`Badge`).
