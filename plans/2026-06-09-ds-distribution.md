# Distribution (распределение ролей %) — DS (только композиция)

**Источник:** Figma «UI фичи» (581:61610 редактор, 582:64731 строка роли, 593:130 «Добавить
роль», 582:64781 буферная область).
**Цель:** редактор распределения ролей по процентам (правило 1.1).

## Аудит (4 ноды) — 100% reuse, новых компонентов НЕТ

| Часть | Решение |
|---|---|
| Строка роли (роль+имя + [+ % −]) | `IncrimentField` (label = 2 строки) |
| Удалить роль | `DeleteButton` |
| Добавить роль | `Button` (tertiary + plus) |
| Буферная область (донат %) | `ProgressRing` |

## Фазы

### [x] Фаза 1. Композиция
- `src/app/ds/distribution-demos.tsx` — строки ролей + добавить + панель ProgressRing.
- Секция «Distribution» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (581:61610 — 1:1).

## Итог

Новых DS-компонентов нет — собрано из `IncrimentField`/`DeleteButton`/`Button`/`ProgressRing`.
Тот же паттерн, что [[2026-06-09-ds-account-edit]] и [[2026-06-09-ds-connection]].
