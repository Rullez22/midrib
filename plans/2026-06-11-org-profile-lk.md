# Профиль кооператива + Лицевой счёт + графики статистики

**Дата:** 2026-06-11
**Источник Figma:** «UI фичи» (fileKey `mmLhl8UT0ROhbyVlt146mT`)
Структура 1791:286201; лицевой счёт 1792:293949 / 293978 / 294017; профиль
пользователя 1792:295866 / 296352 / 294674; профиль кооператива 1795:298288 /
298776 / 299416.

## Что из 10 нод уже было / переиспользовано

| Нода | Решение |
|---|---|
| 1791:286201 Структура | уже = RoleStructure (CKPCard+RoleCard+OrgColumns) |
| 1792:295866/296352/294674 Профиль (Инфо/Лента) | уже сделано (ProfileHeader+секции+Feed) |
| 1795:299416 Лента кооператива | reuse ProfileHeader + FeedComposerBar + FeedPost |
| 1792:293949 Взаиморасчёты | reuse `TransactionsTable` 1:1 |

## Новое

### DS-компоненты
- **ProfileHeader** — `cover` теперь принимает `ReactNode` (обложка-градиент для
  организации); аватар уже принимал ноду (текст-аватар «Immatra»).
- **`stat-charts.tsx`** (новое): `BarChart` (сгруппированные столбцы Пол/Возраст),
  `DonutChart` (кольцевая — Девайсы), `GeoBars` (строки с мини-прогрессом — Гео).
  Презентационные SVG/flex, цвета через CSS-токены.

### Демо-композиции (новых компонентов не требуют)
- **OrgProfileDemos** — профиль кооператива: ProfileHeader(градиент+текст-аватар)
  + ProfileInfoCard (Устав/ОКВЭД) + RequirementsCard + AchievementsCard
  (Самый лучший пайщик) + секция «Статистика» (SectionCard + BarChart +
  DonutChart + GeoBars c тоглом Страны/Города) + Лента.
- **LkDemos** — «Лицевой счёт»: AccountCard + Tabs (Взаиморасчёты/Документооборот/
  Артефакты) + TransactionsTable + таблицы (TableHeader + строки + Badge +
  Combobox + Button). Целиком из готовых DS.

## Фазы

### [x] Фаза 1. ProfileHeader org-вариант (cover: ReactNode)
### [x] Фаза 2. Графики статистики (BarChart / DonutChart / GeoBars)
### [x] Фаза 3. Демо профиля кооператива (+ Статистика, + Лента)
### [x] Фаза 4. Демо «Лицевой счёт» (3 вкладки)
### [x] Фаза 5. Регистрация: export в index.ts; секции «Профиль кооператива» и
«Лицевой счёт» на `/ds`.

## Итог

Реализовано целиком. `tsc --noEmit` чисто; `/ds` 200; скриншоты подтвердили 1:1
(градиент-шапка + текст-аватар, Устав/ОКВЭД, BarChart/DonutChart/GeoBars,
все 3 вкладки Лицевого счёта).

**Примечание:** пред­существующая ошибка гидратации в `RoleDemos`
(page.tsx, `<button>` invalid nesting) сохраняется — к этой задаче не относится,
новые компоненты в стеке ошибки не фигурируют (0 новых ошибок).
