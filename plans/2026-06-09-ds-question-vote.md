# Вопрос (голосование) — DS-компоненты

**Источник:** Figma «UI фичи» — вопрос (nodes 513:0 обзор; просмотр 527:0, 513:1,
575:61505, 579:61604; редактирование 577:61505, 574:61417, 518:59374).
**Цель:** просмотр и редактирование вопроса голосования через DS (правило 1.1).

## Аудит и разбивка (8 нод)

| Режим | Ноды | Решение |
|---|---|---|
| **Просмотр** (параметры вопроса label/value) | 527:0, 513:1, 575:61505, 579:61604 | **Reuse `MemberCard`** (раскрывающаяся карточка + строки label/value; ⓘ в лейбле) |
| **Редактирование** (форма) | 577:61505, 574:61417, 518:59374 | новый `QuestionForm` |
| Обзор | 513:0 | сборка обоих режимов |

## QuestionForm — переиспользует DS

- `Input` — «Название вопроса» + числовые параметры (MIN/MAX продолжительность,
  Кворум, Первоначальный кворум, Консенсус) с иконкой-степпером (`rightIcon`).
- `Textarea` (size l) — «Описание вопроса».
- `Radio` (xs) — «Доступен к передаче?» Да/Нет.
- Select-поле «Тип роли» — Input со стрелкой (переход к выбору роли).
- Токены MIDHUB, без хардкода; Tailwind-утилиты (без нового CSS-файла).

## Фазы

### [x] Фаза 1. Реализация
- `src/components/ds/composite/question-form.tsx` — `<QuestionForm>`.
- Просмотр — через `<MemberCard>` (reuse).
- Экспорт в `index.ts`; демо `src/app/ds/question-vote-demos.tsx` + секция в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (просмотр 527:0 + форма 577:61505 — 1:1).

## Итог

Реализован целиком. Просмотр вопроса — reuse `MemberCard`; редактирование — новый
`QuestionForm` из DS-атомов (`Input`/`Textarea`/`Radio`). Новых базовых атомов не вводилось.
