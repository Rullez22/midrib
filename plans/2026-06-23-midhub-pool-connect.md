# Подключение адресов к пул-счету (по «Перевод» в маркетинговом счёте)

Источник: Figma node 2649:360298. Экран-развилка, открывается по кнопке «Перевод»
в маркетинговом (пул-)счёте: выбор типа подключения адресов к пул-счёту.

## Фаза 1. Экран-развилка [x]

- [x] Скачать иллюстрации карточек из Figma → `public/images/cabinet/connect-mass.svg`,
      `connect-personal.svg`.
- [x] Компонент `PoolConnectScreen` (`/cabinet/account/marketing/connect`): CoopRail +
      крестик закрытия (красный) + заголовок «Подключение адресов к пул-счету» +
      подзаголовок + две `OptionCard` (Массовое / Персональное подключение).
- [x] Reuse DS: `CoopRail`, `OptionCard`, `Button`. Шаблон — `PaymentCreateScreen`.
- [x] В `CabinetScreen` кнопка «Перевод» для `pool`-счёта ведёт на этот экран
      (для целевого — по-прежнему `/cabinet/payment`).

## Осталось

- Цели кнопок «Подключить» (массовое/персональное) — прототип-реакции Figma,
  node-id экранов форм пока нет. Подключить, когда появятся.

## Итог

Реализован экран-развилка подключения адресов к пул-счёту; «Перевод» в маркетинговом
счёте открывает его. Кнопки «Подключить» — заглушки до появления форм.
