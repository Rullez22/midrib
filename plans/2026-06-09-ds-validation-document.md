# Документ на валидацию — DS (только композиция)

**Источник:** Figma «UI фичи» / документ на валидацию (обзор 289:45788; 290:0 кооператив,
654:71 пайщики, 654:1399 вопросы голосований, 656:65209 счета, 656:65319 документы).
**Цель:** экран валидации документа кооператива в DS (правило 1.1).

## Аудит (6 нод) — 100% reuse, новых компонентов НЕТ

Все секции — раскрывающиеся карточки с label/value, т.е. готовый `MemberCard`:

| Секция | Решение |
|---|---|
| Информация о кооперативе (+ бейдж «Локальный») | `MemberCard` + `Badge` |
| Пайщики | `MemberCard` |
| Вопросы голосований (параметры + список под-вопросов) | `QuestionCard` > `MemberCard` + `QuestionCard`×N |
| Счета | `MemberCard` |
| Документы (Сертификат/Свидетельство/Лицензия + дата) | `QuestionCard` + `Item` (overline + дата) |

## Фазы

### [x] Фаза 1. Композиция
- `src/app/ds/validation-demos.tsx` — сборка из `MemberCard`/`QuestionCard`/`Badge`/`Item`.
- Секция «Документ на валидацию» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (290:0, 654:1399 — 1:1).

## Итог

Новых DS-компонентов не вводилось — фича целиком собрана из существующих
(`MemberCard`/`QuestionCard`/`Badge`/`Item`). Показатель зрелости DS.
