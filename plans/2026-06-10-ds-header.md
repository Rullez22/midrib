# Шапка приложения — DS (новый композит Header)

**Источник:** Figma «UI фичи» / header:
- вариант «пространства» — назад + лого + инструменты + навигация + выход (1586:189918)
- вариант «бренд» — лого + вордмарк MIDHUB + навигация + выход (1580:189185)
- вариант «минимальный» — лого + чат + выход (1118:97037)

**Цель:** верхняя шапка как переиспользуемый композит MIDHUB DS (правило 1.1, 9).

## Аудит (3 ноды) — новый композит + набор иконок

| Часть | DS-компонент | reuse |
|---|---|---|
| Полоса шапки (white, h60, нижняя рамка) | — | 🆕 `Header` |
| Навигация (Medium 14/22, grey-90 центр) | `ds-p3-medium` + токены | ✅ токен |
| Вордмарк «MIDHUB» (Medium 24/32) | `ds-h4` | ✅ токен |
| Кнопка-инструмент (56×40, grey-20, рамка) | — | 🆕 `HeaderIconButton` |
| Кнопки чат/выход (32×32, grey-20) | внутр. GhostIconButton | 🆕 внутри Header |
| Лого-аура + иконки (дом/сетка/чат/выход/назад) | — | 🆕 `header-icons` |

Цвета — токены: `bg-surface` (white), `border-border` (#dee5ec),
`bg-surface-muted` (#f3f6f9), `text-foreground` (#242b32),
`text-foreground-subtle` (#93a3b4). Лого — брендовые цвета зашиты (1:1).

## Фазы

### [x] Фаза 1. Иконки
- `src/components/ds/composite/header-icons.tsx` — `MidhubLogo` (аура 32, цвета
  зашиты), `HeaderArrowLeftIcon`, `HeaderHomeIcon`, `HeaderGridIcon`,
  `HeaderChatIcon`, `HeaderExitIcon` (монохром = currentColor). Пути 1:1 из Figma.

### [x] Фаза 2. Композит Header
- `src/components/ds/composite/header.tsx` — props: `logo`, `brand`, `onBack`,
  `leftActions` (слот инструментов, перед ними авто-разделитель), `nav`
  (`HeaderNavItem[]`, абсолютно по центру), `onChat`, `onExit`, `rightActions`.
- Экспорт `Header`, `HeaderIconButton`, типы и иконки в `index.ts`.

### [x] Фаза 3. Витрина
- `src/app/ds/header-demos.tsx` — 3 варианта (пространства / бренд / минимальный).
- Секция «Header» в `src/app/ds/page.tsx` (после DomainNotifications).
- `tsc --noEmit` — чисто, lint — чисто, визуальная проверка /ds — 1:1.

## Итог

Реализован целиком. Новые DS-компоненты: композит `Header` + вспомогательная
`HeaderIconButton` + набор `header-icons`. Слотовый API (`leftActions`/
`rightActions`/`nav`) покрывает все три варианта без дублирования. Только токены
MIDHUB; хардкод — лишь размеры/радиусы 1:1 с Figma (`h-[60px]`, `rounded-[4px]`,
`px-[14px]`) и брендовые цвета логотипа.
