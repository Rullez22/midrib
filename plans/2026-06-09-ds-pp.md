# ПП (производственная программа) — DS

**Источник:** Figma «UI фичи» / ПП (25 нод: 244:42389, 246:54, 251:43009, 249:42965,
253:43200, 385:55498, 2243:233823, 395:1553, 1915:338478/338933/340377, 288:1325, 347:0,
356:124, 385:62391/62956/65161, 623:61911, 348:48245, 393:0, 396:55601/55207, 352:439,
354:500, 395:769).
**Цель:** экраны ПП (документы, согласие, верификация, голосование) в DS (правило 1.1).

## Аудит (25 нод) — 1 новый компонент, остальное reuse

| Часть | Решение |
|---|---|
| Пустое состояние («Отсутствуют документы», «…нажать Продолжить») | новый **`EmptyState`** (иконка + сообщение + действие) |
| Согласие / локализации (флаги + добавить) | `Panel` + `Flag` + `Button` |
| Верификация (тип) | `Badge` (Международная / Жёлтый / Зелёный) |
| Таблицы участников/верификации | `TableHeader` + `Item` |
| Вложенные секции/просмотр ПП | `MemberCard` / `QuestionCard` + `Badge` |
| Согласие-чип | `ToggleButton` (вариант) |

## Фазы

### [x] Фаза 1. Реализация
- `src/components/ds/empty-state.tsx` — `<EmptyState>` (icon/title/description/action).
- Демо `src/app/ds/pp-demos.tsx` — пустые состояния + согласие-локализации (Panel+Flag+Button)
  + просмотр ПП (MemberCard+Badge). Секция «ПП» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (246:54, 251:43009, 249:42965 — 1:1).

## Итог

Новый: `EmptyState`. Остальные экраны ПП — из существующих DS (`Panel`/`Flag`/`Badge`/
`MemberCard`/`QuestionCard`/`TableHeader`/`Item`/`Button`). Представительные собраны в демо;
прочие состояния — композиции тех же компонентов.
