# Коллектив подразделения + профиль подразделения + подписка

**Дата:** 2026-06-11
**Источник Figma:** «UI фичи» (fileKey `mmLhl8UT0ROhbyVlt146mT`), 19 нод.

## Разбор 19 нод

**Уже реализовано ранее / чистый реюз существующих DS (новых компонентов не нужно):**
- 1760:264039, 2017:282684, 1766:269126 Структура → RoleStructure (CKPCard/RoleCard/OrgColumns)
- 1562:184828/185280/185443, 1768:270389/270514/270689, 2193:230186 Лицевой счёт / Счета →
  AccountCard · Tabs · TransactionsTable · TableHeader · Item · Badge · Combobox · Button
- 1763:264839/265464 профиль пользователя (Инфо/Лента) → ProfileHeader + секции + Feed
- 1873:298531 CKPCard (подразделение)

**Новое в этой задаче:**
1. **TeamMemberCard** (`composite/team-member-card.tsx`) — карточка участника
   «Коллектив подразделения»: фото 155×128 / пустое состояние (warning + «Нет
   пайщика»), статус-бейдж наезжает на низ фото (active green-300 / pending
   yellow-300 / inactive red-300), иконка-меню, имя+роль по центру, рамка red-200
   (selected red-400). 1:1 с 2023:223041 / 2024:226388.
2. **ProfileHeader** — добавлен проп `actions?: ReactNode` (заменяет дефолтную
   кнопку «Поделиться профилем») → вариант «+ Подписаться» (1762:264757,
   1763:264839, 1781:285025…). `cover` уже принимал ноду (градиент).

**Демо (`team-demos.tsx`):**
- Коллектив подразделения: CKPCard (лого + «20 пайщиков») + ряд TeamMemberCard
  (пусто ×2 + active/pending/active, один selected).
- Профиль подразделения: ProfileHeader (cover-градиент + групповой аватар +
  «Подписаться») + Banner «Хотите стать пайщиком…/Оставить заявку» +
  ProfileInfoCard + RequirementsCard + AchievementsCard.

## Фазы
### [x] Фаза 1. TeamMemberCard
### [x] Фаза 2. ProfileHeader.actions (Подписаться)
### [x] Фаза 3. Демо коллектива + профиля подразделения; export + секция на /ds

## Итог

Реализовано. `tsc --noEmit` чисто; `/ds` 200; скриншот подтвердил 1:1
(TeamMemberCard все статусы + пусто + selected, CKPCard, шапка-градиент с
«Подписаться», Banner вступления). 0 новых ошибок гидратации (сохраняется
пред­существующая в RoleDemos, не относится к задаче).

## Не собрано отдельным демо (по запросу — могу добавить)
- Полная версия «Взаиморасчёты» (ReportPeriodBar + StatSummary + под-табы
  Все/Входящие/Исходящие) — все части в DS уже есть.
- Экран «Счета» (86-й/Административные/Паевые + «Целевой счёт» + строки «10 % …
  Подробнее» + drill-in «Доля в счёте») — композиция Tabs · AccountCard · Item ·
  TransactionsTable.
