# Footer — панель действий (композит DS)

**Источник:** Figma «UI фичи» / footer component (node `1987:197573`).
**Цель:** панель действий с кнопками — композит `<Footer>` 1:1 с дизайном.

Композит в `src/components/ds/composite/` (см. правило 9 в `docs/MIDHUB_WORKFLOW_RULES.md`).

## Матрица дизайна

- **Полоса:** фон `grey-20`, рамка `grey-90`, радиус 4, padding 16/24, gap 24.
- **Высота — от размера кнопок:** L-48 → ~80, M-40 → ~72, S-32 → ~64 (контентная).
- **align:** `end` (по умолч.) · `start` · `between`.
- **attached:** футер карточки — скругление только снизу + компактный паддинг (8).
- Кнопки — DS `<Button>`: L две (`secondary` «Редактировать» + `primary` «Подписать»),
  M/S одна `primary` «Опубликовать», attached — `tertiary` «Редактировать» слева.

## Использованные DS-компоненты

- `<Button>` (variant/size) — все действия. Токены MIDHUB: `grey-20/90`, `--radius-xs`.

## Фазы

### [x] Фаза 1. Композит и стили
- `src/components/ds/composite/footer.tsx` — `<Footer>` (`align`, `attached`, children).
- `src/components/ds/composite/footer.css` — `.ds-footer*` в `@layer components`.
- Регистрация CSS в `globals.css`; экспорт в `index.ts`.

### [x] Фаза 2. Витрина и проверка
- `src/app/ds/footer-demos.tsx` (L / M / S / attached) + секция «Footer» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено с Figma (1:1).

## Итог

Реализован целиком. `<Footer>` повторяет варианты L-48/M-40/S-32/редактировать
(1987:197573) 1:1. Собран из DS `<Button>` и токенов MIDHUB.
