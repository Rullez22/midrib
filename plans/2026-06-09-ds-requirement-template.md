# Требования шаблон — DS (только композиция)

**Источник:** Figma «UI фичи» / требования шаблон (449:116800, 452:78666, 455:78802,
455:79134, 452:78671, 452:78712).
**Цель:** карточка требования валидатора в шаблоне (правило 1.1).

## Аудит (6 нод) — 100% reuse, новых компонентов НЕТ

Полная карточка требования = `QuestionCard` (раскрытие) + `Badge` (Жёлтый/Зелёный,
Международный) + label/value (Описание, Тип верификации) + `Item` (документы) +
строки Выдано/Использование/Стоимость. Совпадает с карточками из разделов
[[2026-06-09-ds-requirements]] и [[2026-06-09-ds-template-builder]].

## Фазы

### [x] Фаза 1. Композиция
- `src/app/ds/requirement-template-demos.tsx` — полная раскрытая карточка + свёрнутая.
- Секция «Требование (шаблон)» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (455:78802 — 1:1).

## Итог

Новых DS-компонентов нет — карточка собрана из `QuestionCard`/`Badge`/`Item`.
