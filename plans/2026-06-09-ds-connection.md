# Подключение пайщиков (массовое/персональное · разовое/стабильное) — DS

**Источник:** Figma «UI фичи» (967:90161/90164/90186, 968:90510, 970:90321, 1460:177078,
1461:177121, 972:90307, 973:91180, 975:109181, 979:92303, 2206:231117, 1468:183455,
1472:204508, 1478:197920).
**Цель:** флоу подключения пайщиков в DS (правило 1.1).

## Аудит (15 нод) — 100% reuse, новых компонентов НЕТ

| Часть | Решение |
|---|---|
| Выбор участников (вкладки + таблица + пагинация) | `Tabs` + `TableHeader` + `Item` (Checkbox+star+link+ENG) + `Pagination` |
| Основания/строки пула | `Item` / `TableHeader` |
| Распределение (Целевой/токены, %) | `IncrimentField` |
| Счёт пайщика (процент + дни + сумма) | `ProgressRing` + `Checkbox` + `Input` |
| Итог по платежу (получатели/сумма) | label/value (`Text`) |

## Фазы

### [x] Фаза 1. Композиция
- `src/app/ds/connection-demos.tsx` — представительная сборка (таблица участников,
  стабильное подключение, итог) из готовых DS.
- Секция «Подключение пайщиков» в `page.tsx`.
- `tsc --noEmit` — чисто; визуально сверено (972:90307, 1468:183455, 1472:204508 — 1:1).

## Итог

Новых DS-компонентов не вводилось — флоу собран из существующих
(`Tabs`/`TableHeader`/`Item`/`Pagination`/`IncrimentField`/`ProgressRing`/`Checkbox`/`Input`).
